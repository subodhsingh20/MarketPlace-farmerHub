services:
  backend:
    image: subodhsingh20/farmer-marketplace-backend:latest
    ports:
      - "5000:5000"
    environment:
      - CLOUDANT_URL=${CLOUDANT_URL}
      - CLOUDANT_API_KEY=${CLOUDANT_API_KEY}
      - CLOUDANT_DB_PREFIX=${CLOUDANT_DB_PREFIX:-farmer_marketplace}
      - CLIENT_URL=${CLIENT_URL}
      - JWT_SECRET=${JWT_SECRET}
      - PAYMENT_MODE=${PAYMENT_MODE:-live}
      - RAZORPAY_KEY_ID=${RAZORPAY_KEY_ID}
      - RAZORPAY_KEY_SECRET=${RAZORPAY_KEY_SECRET}
      - NODE_ENV=production
    networks:
      - app-network
    restart: unless-stopped

  frontend:
    image: subodhsingh20/farmer-marketplace-frontend:latest
    ports:
      - "80:80"
    depends_on:
      - backend
    networks:
      - app-network
    restart: unless-stopped

networks:
  app-network:
    driver: bridge
